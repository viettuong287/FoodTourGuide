<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:zxing="clr-namespace:ZXing.Net.Maui.Controls;assembly=ZXing.Net.MAUI.Controls"
             x:Class="VinhThucAudioGuide.QrPage"
             Title="Mã QR"
             BackgroundColor="#1C1C1E">
    <Grid RowDefinitions="*, Auto">

        <zxing:CameraBarcodeReaderView 
            x:Name="CameraReader"
            Grid.Row="0"
            BarcodesDetected="CameraReader_BarcodesDetected"
            IsDetecting="True"
            Margin="20" />

        <VerticalStackLayout Grid.Row="1" Padding="20" Spacing="10" BackgroundColor="#1C1C1E">
            <Label Text="Quét mã QR tại điểm du lịch" 
                   TextColor="White" 
                   FontSize="20" 
                   FontAttributes="Bold" 
                   HorizontalOptions="Center" />

            <Label Text="Hãy đưa camera lại gần mã QR để nghe thuyết minh chi tiết về địa danh này." 
                   TextColor="Gray" 
                   FontSize="14" 
                   HorizontalTextAlignment="Center" 
                   HorizontalOptions="Center" />
        </VerticalStackLayout>

    </Grid>
</ContentPage>